<UserControl x:Class="UserControls.Button.LevButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" HorizontalAlignment="Right" VerticalAlignment="Center">
    <Button Width="72" Height="72" Click="btn_Click">
        <Button.Template>
            <ControlTemplate>
                <Grid>
                    <Border CornerRadius="36" Background="Black" Opacity="0.1" >
                    </Border>
                    <Border CornerRadius="31" Width="62" Height="62" Background="Black" Opacity="0.11" >
                    </Border>
                    <Border CornerRadius="25" Width="50" Height="50" Background="White" Opacity="0.5" >
                        <TextBlock Text="输入按键" VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </ControlTemplate>
        </Button.Template>
    </Button>
</UserControl>