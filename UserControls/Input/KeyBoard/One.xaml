<UserControl x:Class="UserControls.Input.KeyBoard.One"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             Name="self"
             Height="50" Width="50">
    <Grid PreviewMouseDown="Grid_PreviewMouseDown">
        <Border  Opacity="0.01" Background="{Binding RelativeSource={RelativeSource Mode=FindAncestor,AncestorType=UserControl},Path=Background}"></Border>
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" Text="{Binding ElementName=self,Path=Z}" />
        <TextBlock Margin="3" HorizontalAlignment="Left" VerticalAlignment="Bottom" Foreground="IndianRed" Text="{Binding  ElementName=self,Path=Num}" />
    </Grid>
</UserControl>
